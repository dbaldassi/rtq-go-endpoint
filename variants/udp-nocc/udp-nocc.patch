diff --git a/tools/run_endpoint.sh b/tools/run_endpoint.sh
index 8cb3cc0..1342fa0 100644
--- a/tools/run_endpoint.sh
+++ b/tools/run_endpoint.sh
@@ -10,8 +10,8 @@ if [ "$ROLE" == "sender" ]; then
     # Wait for the simulator to start up.
     #/wait-for-it.sh sim:57832 -s -t 10
     echo "Starting RTQ sender..."
-    QUIC_GO_LOG_LEVEL=error ./rtq send -addr $RECEIVER $SENDER_PARAMS $VIDEOS
+    QUIC_GO_LOG_LEVEL=error ./rtq send -addr $RECEIVER -transport udp $SENDER_PARAMS $VIDEOS
 else
     echo "Running RTQ receiver."
-    QUIC_GO_LOG_LEVEL=error ./rtq receive $RECEIVER_PARAMS $DESTINATION
+    QUIC_GO_LOG_LEVEL=error ./rtq receive -transport udp $RECEIVER_PARAMS $DESTINATION
 fi
